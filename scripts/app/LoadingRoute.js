define(["ember"], function(Ember){
	var LoadingRoute = Ember.Route.extend({
//		actions: {
//			error: function(reason, transition) {
//				Ember.Logger.error('LoadingRoute error received:',reason,transition);
////				this.controllerFor('banner').displayError(reason);
//			}
//		}
	});
	return LoadingRoute;
});